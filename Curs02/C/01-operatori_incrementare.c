/* Programarea Calculatoarelor CA, 2019-2020
   Exemple cu operatori de incrementare / decrementare pre și post fixată  */

/*  Operatorul de incrementare este ++, iar cel de decremetare este --
	Fiecare dintre acestia pot fi pre si post fixati;
	Pre : ++a;
	Post: b--;
	In cazul operatorului pre fixat, mai intai se realizeaza incrementare,
	respectiv decrementarea, iar apoi se lucreaza cu noua valoare
	In cazul operatorului post fixat, mai intai se lucreaza cu valoarea, iar
	abia dupa este incrementata, respectiv decrementata
	Ex : x = 7;
		 ++x; (x = 8)
		 x++; (x = 9);

		 a = 2;
		 b = a++; => b = 2, a = 3;

		 c = 14;
		 d = --c; => c = 13, d = 13;  */

int main(void)
{
	return 0;
}
